# WISPERMO runtime deps for macOS
faster-whisper==1.2.1
PySide6==6.11.1
sounddevice          # CoreAudio capture
pynput               # global hotkey + keystroke typing (pulls pyobjc on macOS)

# Build-only: pip install pyinstaller
